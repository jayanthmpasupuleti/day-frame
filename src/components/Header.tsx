import React from 'react';
import { TitleBar } from './TitleBar';

/**
 * Header component aliases and provides the top navigation bar with Theme Switcher,
 * Window dragging, live Pomodoro pill, and Ambient Focus Audio.
 */
export const Header: React.FC = () => {
  return <TitleBar />;
};

export default Header;
